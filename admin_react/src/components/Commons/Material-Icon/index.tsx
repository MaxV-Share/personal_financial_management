export interface IMaterialIconProps {
  name: string;
  index?: number;
}

// export function MaterialIcon(props: IMaterialIconProps) {
//   const { name, index } = props;
//   var icon = require(`@mui/icons-material/${name}`);
//   return <>{icon}</>;
// }
